name: morning
on:
  schedule:
  - cron:  '0 0 * * *'
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    name: send morning to your girlfriend

    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        ref: master

    - name: sender
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: pip install -r ./requirements.txt && python ./main.py

    env:
      APP_ID: ${{ secrets.wx61bd9e2ae0f1d4c6 }}
      APP_SECRET: ${{ secrets.a492d36e6c7a6dfbc2f00e80b38b41a1 }}
      TEMPLATE_ID: ${{ secrets.	sh6aFwdqkcwedTwtVidCuIg3JnhEN_wr0BiabdLFgiA }}
      USER_ID: ${{ secrets.ogCgI6o8-Pebb2T160JJuQKVnPGw }}
      START_DATE: ${{ secrets.2022-08-25 }}
      BIRTHDAY: ${{ secrets.01-20 }}
      CITY: ${{ secrets.保定 }}
